# I believe this is where we're supposed to put the hosts that we actually want to connect to for staging
---
all:
  children:
    app_servers:
      hosts:
        app_staging_1:
          ansible_host: 34.205.85.247
          ansible_user: platform-capstone-bastion
          ansible_ssh_private_key_file: ~/.ssh/id_rsa