# I believe this is where we're supposed to put the hosts that we actually want to connect to for staging
---
all:
  children:
    app_servers:
      hosts:
        app_staging_1:
          ansible_host: 3.238.2.42
          ansible_user: ec2-user
          ansible_ssh_private_key_file: ~/.ssh/capstonekey.pem
