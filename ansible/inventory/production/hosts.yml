# I believe this is where we're supposed to put the hosts that we actually want to connect to for production
---
all:
  children:
    app_servers:
      hosts:
        tag_Role_bastion:tag_Role_private_app:
          ansible_host: 3.235.17.40 #Whichever one we're connecting to, Terraform people might know
          ansible_user: ec2-user
          ansible_ssh_private_key_file: ~/.ssh/capstonekey.pem
          ansible_python_interpreter: /usr/local/bin/python3.11

    tag_Role_prometheus:
      hosts:
        prometheus_staging:
          ansible_host: 10.1.2.237
          ansible_user: ec2-user
          ansible_ssh_private_key_file: ~/.ssh/capstonekey.pem
          ansible_python_interpreter: /usr/local/bin/python3.11

    tag_Role_grafana:
      hosts:
        grafana_staging:
          ansible_host: 3.85.228.141
          ansible_user: ec2-user
          ansible_ssh_private_key_file: ~/.ssh/capstonekey.pem
          ansible_python_interpreter: /usr/local/bin/python3.11
    
    tag_Role_router:
      hosts:
        router_staging:
          ansible_host: 54.83.75.12
          ansible_user: ec2-user
          ansible_ssh_private_key_file: ~/.ssh/capstonekey.pem
          ansible_python_interpreter: /usr/local/bin/python3.11
    
